# Blender Demo Requirements
# ===========================
# Single source of truth: ../pyproject.toml
#
# To install all dependencies:
#   pip install -e ..[dev]        (from project root - recommended)
#
# Or use this file for compatibility:
#   pip install -r requirements.txt
#
# Note: Dependencies below are mirrored from pyproject.toml.
# Update pyproject.toml when changing versions.

# Core dependencies (from pyproject.toml)
numpy==1.26.4
pillow==10.3.0

# For local Blender wheel:
# pip install /path/to/blender_bpy_module-4.4.whl